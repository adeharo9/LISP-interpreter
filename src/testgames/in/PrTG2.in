(define diff (x y) (+ x (- y)))

(define fibonacci (a) (if (< a 2)) 1 (+ (fibonacci (diff a 1)) (fibonacci (diff a 2)))))

(define null (x) (= x ()))
(define rest (x) (tail x))
(define 1+ (x) (+ x 1))


(define length (L) (if (= L ()) 0 (+ 1 (length (tail L)))))